namespace SnapdragonProfilerCLI.Logging
{
    /// <summary>
    /// Log severity levels (distinct from QGLPlugin's LogLevel enum).
    /// </summary>
    public enum AppLogLevel
    {
        Debug   = 0,
        Info    = 1,
        Success = 2,
        Warn    = 3,
        Error   = 4
    }
}
